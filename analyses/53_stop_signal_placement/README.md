# 53 - Near-Side vs. Far-Side Stop Placement

Classifies each PRT bus stop as near-side (before the traffic light), far-side (after it), or mid-block using GTFS shapes and OSM signal locations. 83% of stops at signalized intersections are near-side — the legacy default — vs. 17% far-side.

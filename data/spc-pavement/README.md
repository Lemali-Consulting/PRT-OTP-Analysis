# SPC NHS Pavement-Condition Data

Cached pavement-condition GeoJSON for the National Highway System in the SPC
region (pavement roughness / IRI, overall pavement index, condition rating, and
geometry). The raw GeoJSON is downloaded from SPC's ArcGIS service by
`road_overlay_pavement.py` and is gitignored.

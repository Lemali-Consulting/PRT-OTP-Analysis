# NTD Fleet Age Data

Vehicle age distribution data from the NTD Annual Database, sourced via the Socrata API at `data.transportation.gov` (dataset `6abt-uhgq`). Provides average fleet age by vehicle type per transit agency for report years 2022–2024.

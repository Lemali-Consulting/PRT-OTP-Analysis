# 11 - Signal Overlay

Fetches OpenStreetMap traffic-signal nodes and spatially matches them to GTFS route geometry to compute per-route signal counts and signal density. Writes `route_signals` into `prt.db`.

# 01 - Data Ingestion

Builds `data/prt.db` from the canonical local CSV extracts and normalizes route, stop, OTP, and ridership tables. This is the pipeline entrypoint required before running analyses from a clean checkout.

# 02 - Scheduled Trips

Loads monthly scheduled trip counts from WPRDC and writes `scheduled_trips_monthly` plus `schedule_periods` into `data/prt.db`. This step enables analyses that require time-varying service levels.

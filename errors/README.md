# Errors

Structured error reports organized by class (e.g., `data-quality/`, `tooling/`). Each report documents what went wrong, how it was discovered, and how it was fixed. Use `uv run python tools/error_report.py add <class> "title"` to create a new report.

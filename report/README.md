# Report

Generates a single self-contained HTML report combining all 18 analysis findings, embedded charts, and appendices. Run `uv run python report/build_report.py` from the project root to build.

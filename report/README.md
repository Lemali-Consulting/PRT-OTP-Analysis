# Report

Generates a single self-contained stakeholder HTML report from analysis findings, embedded charts, and appendices.

This is the legacy report surface. The scaffold-aligned project website now lives under `products/website/`.

Build commands:

- `uv run python report/build_report.py` (legacy single-report output)
- `uv run python products/website/main.py` (scaffold-aligned static site)
